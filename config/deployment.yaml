apiVersion: apps/v1
kind: Deployment
metadata:
  name: catapp
  labels:
    app: catapp
spec:
  replicas: 1
  selector:
    matchLabels:
      app: catapp
  template:
    metadata:
      labels:
        app: catapp
    spec:
      containers:
        - name: catapp
          image: catapp:latest
          imagePullPolicy: Never
          ports:
            - containerPort: 3000
              protocol: TCP
